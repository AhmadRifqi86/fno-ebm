\section{Related Works}

\subsection{Neural Operators}

Neural operators extend traditional neural networks from learning mappings between finite-dimensional vectors to
learning mappings between infinite-dimensional function spaces \cite{lu2021deeponet,chen1995universal,kovachki2023neural}. Unlike 
standard neural networks that approximate functions $f: \mathbb{R}^n \rightarrow \mathbb{R}^m$, neural operators 
learn solution operators $\mathcal{G}: \mathcal{A} \rightarrow \mathcal{U}$ that map input functions to output 
functions, enabling mesh-independent PDE solving with discretization-invariant generalization.

Fourier Neural Operators (FNOs) \cite{li2020fourier} have emerged as the most successful neural operator 
architecture, leveraging spectral convolutions in Fourier space to efficiently capture global dependencies. FNOs 
have demonstrated state-of-the-art performance across diverse PDEs including Navier-Stokes equations, Darcy flow, 
and Burgers equation, achieving up to 1000$\times$ speedup compared to traditional numerical solvers. Since the 
original FNO, numerous extensions have been proposed: Factorized FNO \cite{tran2021factorized} for high-dimensional 
efficiency, U-shaped FNO \cite{wen2022u} for multi-scale features, and Geo-FNO \cite{li2022fourier} for arbitrary 
geometries. However, none of these works address uncertainty quantification, focusing instead on improving point 
prediction accuracy.

\subsection{Uncertainty Quantification in Deep Learning}

Uncertainty quantification aims to not only predict outputs but also quantify prediction confidence
\cite{abdar2021review}. In the Bayesian framework \cite{bishop2006pattern}, we seek the posterior predictive
distribution by marginalizing over model parameters. However, exact Bayesian inference is intractable for neural
networks, leading to various approximation methods.

\textbf{Monte Carlo Dropout} \cite{gal2016dropout} provides a theoretical foundation linking dropout regularization to approximate Bayesian inference. The key insight is that a neural network trained with dropout can be interpreted as an approximation to a deep Gaussian process \cite{damianou2013deep}. At test time, instead of disabling dropout, it remains active across $T$ stochastic forward passes, each producing a different prediction due to the random zeroing of neurons, capturing model uncertainty from the stochastic dropout masks. However, MC Dropout has notable limitations: it generally yields poorly calibrated uncertainties \cite{ovadia2019can}, the quality depends heavily on dropout rate tuning, and it requires $T$ forward passes at inference time---negating the computational advantages of neural operators designed for fast PDE solving.

\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=0.8cm and 1cm,
    model/.style={rectangle, draw, minimum width=1.5cm, minimum height=1.2cm, align=center, fill=white, font=\scriptsize},
    pass/.style={rectangle, draw, minimum width=1cm, minimum height=0.8cm, align=center, fill=white, font=\scriptsize},
    result/.style={rectangle, draw, minimum width=1.8cm, minimum height=0.8cm, align=center, fill=white, font=\scriptsize}
]
\node[model] (model) {Trained\\FNO Model\\(with Dropout)};
\node[above=0.4cm of model, font=\scriptsize] (input) {$a(x)$};
\draw[->, thick] (input) -- (model);
\node[pass, below right=0.4cm and 0.3cm of model] (p1) {Pass 1};
\node[pass, right=0.2cm of p1] (p2) {Pass 2};
\node[pass, right=0.2cm of p2] (p3) {...};
\node[pass, right=0.2cm of p3] (p30) {Pass 30};
\draw[->, thick] (model.east) -| (p1.north);
\draw[->, thick] (model.east) -| (p2.north);
\draw[->, thick] (model.east) -| (p3.north);
\draw[->, thick] (model.east) -| (p30.north);
\node[below=0.3cm of p1, font=\scriptsize] (o1) {$\hat{y}_1$};
\node[below=0.3cm of p2, font=\scriptsize] (o2) {$\hat{y}_2$};
\node[below=0.3cm of p3, font=\scriptsize] (o3) {...};
\node[below=0.3cm of p30, font=\scriptsize] (o30) {$\hat{y}_{30}$};
\draw[->, thick] (p1) -- (o1);
\draw[->, thick] (p2) -- (o2);
\draw[->, thick] (p30) -- (o30);
\node[result, below=1.35cm of p2] (mean) {Mean: $\mu = \frac{1}{T}\sum \hat{y}_t$};
\node[result, below=0.2cm of mean] (var) {Var: $\sigma^2 = \frac{1}{T}\sum (\hat{y}_t - \mu)^2$};
\draw[->, thick, dashed] (o1.south) -- ++(0,-0.4) -| (mean.north);
\draw[->, thick, dashed] (o2.south) -- ++(0,-0.4) -| (mean.north);
\draw[->, thick, dashed] (o30.south) -- ++(0,-0.4) -| (mean.north);
\end{tikzpicture}%
}
\caption{Monte Carlo Dropout inference with T=30 stochastic forward passes.}
\label{fig:mc_dropout}
\end{figure}

\textbf{Deep Ensembles} \cite{lakshminarayanan2017simple} take a frequentist approach to uncertainty quantification by training $M$ independent networks, each with different random weight initializations. The non-convex loss landscape of neural networks means that different initializations converge to different local minima \cite{fort2019deep}, creating an implicit sampling over the function space. Given an input, each ensemble member produces a prediction, and uncertainty is estimated from their disagreement. The variance directly measures epistemic uncertainty: regions where all models agree yield low variance, while regions outside the training distribution cause models to diverge. This approach is widely regarded as the gold standard for uncertainty quantification \cite{ovadia2019can,fort2019deep}, consistently outperforming other methods in calibration benchmarks. However, ensembles incur $M$-fold computational overhead in both training and inference, making them impractical for real-time applications where neural operators are most valuable.

\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=1cm and 0.3cm,
    model/.style={rectangle, draw, minimum width=1.2cm, minimum height=1.5cm, align=center, rounded corners, font=\scriptsize},
    result/.style={rectangle, draw, minimum width=2cm, minimum height=0.6cm, align=center, fill=white, font=\scriptsize}
]
\node[font=\scriptsize] (input) {$a(x)$};
\node[model, fill=white, below right=0.3cm and -0.5cm of input] (m1) {\textbf{M1}\\$\hat{y}_1$};
\node[model, fill=white, right=0.2cm of m1] (m2) {\textbf{M2}\\$\hat{y}_2$};
\node[model, fill=white, right=0.2cm of m2] (m3) {\textbf{M3}\\$\hat{y}_3$};
\node[model, fill=white, right=0.2cm of m3] (m4) {\textbf{M4}\\$\hat{y}_4$};
\node[model, fill=white, right=0.2cm of m4] (m5) {\textbf{M5}\\$\hat{y}_5$};
\draw[->, thick] (input) -| (m1.north);
\draw[->, thick] (input) -| (m2.north);
\draw[->, thick] (input) -| (m3.north);
\draw[->, thick] (input) -| (m4.north);
\draw[->, thick] (input) -| (m5.north);
\node[result, below=1cm of m3] (mean) {Mean: $\mu = \frac{1}{M}\sum \hat{y}_m$};
\node[result, below=0.2cm of mean] (var) {Var: $\sigma^2 = \frac{1}{M}\sum (\hat{y}_m - \mu)^2$};
\draw[->, thick, dashed] (m1.south) -- ++(0,-0.2) -| (mean.north);
\draw[->, thick, dashed] (m2.south) -- ++(0,-0.2) -| (mean.north);
\draw[->, thick, dashed] (m3.south) -- ++(0,-0.2) -| (mean.north);
\draw[->, thick, dashed] (m4.south) -- ++(0,-0.2) -| (mean.north);
\draw[->, thick, dashed] (m5.south) -- ++(0,-0.2) -| (mean.north);
\end{tikzpicture}%
}
\caption{Deep Ensemble with M=5 independently trained models.}
\label{fig:deep_ensemble}
\end{figure}

\textbf{Evidential Deep Learning} \cite{sensoy2018evidential,amini2020deep} offers a fundamentally different approach by enabling deterministic uncertainty quantification in a single forward pass. Rather than sampling from weight distributions or aggregating multiple models, EDL trains the network to output the hyperparameters of a higher-order evidential distribution---specifically, the Normal-Inverse-Gamma (NIG) distribution for regression tasks. This formulation leverages conjugate prior theory from Bayesian statistics, allowing the network to directly parameterize a distribution over possible Gaussian likelihoods. The key advantage is analytical tractability: uncertainty naturally decomposes into aleatoric and epistemic components without requiring ensemble disagreement or stochastic sampling. This makes EDL particularly attractive for neural operators where computational efficiency is paramount.

\subsection{Known Limitations of Evidential Methods}

Multiple studies \cite{jurgens2024epistemic,ulmer2024prior,bickford2024rethinking} have shown that the theoretical 
decomposition of uncertainty into epistemic and aleatoric components does not work correctly in practice---both 
uncertainty types decrease with training data, violating the assumption that aleatoric uncertainty represents 
irreducible noise. Furthermore, empirical studies \cite{meinke2024uncertaintyquantification,hullermeier2021aleatoric} 
consistently find that evidential methods achieve near-random performance (AUROC $\sim$ 0.5-0.6) on 
out-of-distribution detection, limiting their utility for safe deployment.

\subsection{Gap in Literature}

Despite extensive work on UQ for standard neural networks \cite{abdar2021review}, only a few papers address UQ
for neural operators. Geneva and Zabaras \cite{geneva2022modeling} propose variational inference for FNOs requiring
expensive MCMC sampling. Psaros et al. \cite{psaros2023uncertainty} develop Bayesian DeepONet using Hamiltonian
Monte Carlo at prohibitive computational cost. No prior work systematically compares MC Dropout, Deep Ensembles,
and Evidential Deep Learning for neural operators, nor investigates whether known evidential limitations extend to
PDE solving \cite{karniadakis2021physics}---characterized by continuous function approximation, physics-informed
structure, and unlimited synthetic training data.


\section{Preliminaries}
\label{sec:preliminaries}

\subsection{Fourier Neural Operator}

Given an input function $a(x)$ and output function $u(x)$ related by a PDE operator $\mathcal{L}$, a neural 
operator $\mathcal{G}_\theta$ learns to approximate the solution operator:
\begin{equation}
    \mathcal{G}_\theta: a \mapsto u \quad \text{such that} \quad \mathcal{L}(u; a) = 0
\end{equation}

The FNO parameterizes this mapping using spectral convolutions. Writing $v_l$ for the feature function entering
layer $l$, the key component is the spectral convolution layer:
\begin{equation}
    (\mathcal{K}(R) \cdot v_l)(x) = \mathcal{F}^{-1}\left( R \cdot \mathcal{F}(v_l) \right)(x)
\end{equation}
where $\mathcal{F}$ denotes the Fourier transform, $\mathcal{F}^{-1}$ the inverse transform, and $R$ are learnable weights truncated to low Fourier modes. Throughout, $a$ denotes an input function of the operator, $u$ the PDE solution, and $v_l$ an intermediate feature; this convention is used consistently in Fig.~\ref{fig:spectral_convolution} and Fig.~\ref{fig:fno_architecture}.

\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=0.8cm,
    box/.style={rectangle, draw, minimum width=2.2cm, minimum height=2.2cm, align=center, font=\small},
    smallbox/.style={rectangle, draw, minimum width=1.2cm, minimum height=1.2cm, align=center, font=\scriptsize},
    arrow/.style={->, >=stealth, thick},
    label/.style={font=\footnotesize, align=center}
]

% ===== Input v_l(x) in spatial domain =====
\node[box, fill=white] (input) {
    \textbf{Input}\\[2pt]
    $v_l(x)$\\[4pt]
    \begin{tikzpicture}[scale=0.4]
        \foreach \x in {0,0.3,...,1.5} {
            \foreach \y in {0,0.3,...,1.5} {
                \pgfmathsetmacro{\c}{min(100,max(0,50+40*sin(\x*180)*cos(\y*180)))}
                \fill[blue!\c!white] (\x,\y) rectangle (\x+0.3,\y+0.3);
            }
        }
        \draw[black] (0,0) rectangle (1.8,1.8);
    \end{tikzpicture}
};
\node[label, below=0.1cm of input] {Spatial\\$s \times s$};

% ===== Fourier Space (full) =====
\node[box, fill=white, right=1.5cm of input] (fourier) {
    \textbf{Fourier}\\[2pt]
    $\hat{v}_l(k)$\\[4pt]
    \begin{tikzpicture}[scale=0.4]
        \fill[purple!40] (0,0) rectangle (1.8,1.8);
        \fill[yellow!60] (0.5,0.5) rectangle (1.3,1.3);
        \draw[green!70!black, line width=1pt] (0.5,0.5) rectangle (1.3,1.3);
        \draw[black] (0,0) rectangle (1.8,1.8);
    \end{tikzpicture}
};
\node[label, below=0.1cm of fourier] {Spectral\\$k_{\max}$ modes};

% ===== Learnable Weights R =====
\node[box, fill=white, right=1.5cm of fourier] (weights) {
    \textbf{Filtered}\\[2pt]
    $R \odot \hat{v}_l$\\[4pt]
    \begin{tikzpicture}[scale=0.4]
        \fill[gray!20] (0,0) rectangle (1.8,1.8);
        \fill[orange!50] (0.5,0.5) rectangle (1.3,1.3);
        \draw[red!70!black, line width=1pt] (0.5,0.5) rectangle (1.3,1.3);
        \draw[black] (0,0) rectangle (1.8,1.8);
    \end{tikzpicture}
};
\node[label, below=0.1cm of weights] {Learnable\\$R$};

% ===== Output v_{l+1}(x) =====
\node[box, fill=white, right=1.5cm of weights] (output) {
    \textbf{Output}\\[2pt]
    $v_{l+1}(x)$\\[4pt]
    \begin{tikzpicture}[scale=0.4]
        \foreach \x in {0,0.3,...,1.5} {
            \foreach \y in {0,0.3,...,1.5} {
                \pgfmathsetmacro{\c}{min(100,max(0,30+50*sin(\x*120)*cos(\y*150)))}
                \fill[green!\c!white] (\x,\y) rectangle (\x+0.3,\y+0.3);
            }
        }
        \draw[black] (0,0) rectangle (1.8,1.8);
    \end{tikzpicture}
};
\node[label, below=0.1cm of output] {Spatial\\$s \times s$};

% ===== Arrows connecting boxes =====
\draw[arrow, line width=1.5pt, blue!70] (input.east) -- node[above, font=\scriptsize\bfseries] {FFT} (fourier.west);
\draw[arrow, line width=1.5pt, purple!70] (fourier.east) -- node[above, font=\scriptsize\bfseries] {$\odot$} (weights.west);
\draw[arrow, line width=1.5pt, green!60!black] (weights.east) -- node[above, font=\scriptsize\bfseries] {IFFT} (output.west);

% ===== Bottom equation =====
\node[below=0.8cm of fourier, font=\small, xshift=1cm] {
    $\mathcal{K}(v_l)(x) = \mathcal{F}^{-1}\big( R \cdot \mathcal{F}(v_l) \big)(x)$
};

\end{tikzpicture}%
}
\caption{Spectral convolution in FNO. The layer input $v_l(x)$ is transformed via FFT, low-frequency modes are filtered by learnable weights $R$, then IFFT returns to the spatial domain, giving $v_{l+1}(x)$. Symbols match Eq.~(2).}
\label{fig:spectral_convolution}
\end{figure}

The FNO architecture consists of three stages:
\begin{enumerate}
    \item \textbf{Lifting}: Project input to latent space via $v_0(x) = P(a(x))$
    \item \textbf{Fourier Layers}: Apply $L$ spectral convolution layers with skip connections:
    \begin{equation}
        v_{l+1}(x) = \sigma\left( \mathcal{K}(\mathbf{w}_l) \cdot v_l(x) + W_l v_l(x) \right)
    \end{equation}
    \item \textbf{Projection}: Map to output space: $u(x) = Q(v_L(x))$
\end{enumerate}

\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=0.6cm and 0.8cm,
    layer/.style={rectangle, draw, minimum width=1cm, minimum height=1.2cm, align=center, font=\scriptsize, rounded corners=2pt},
    arrow/.style={->, >=stealth, thick}
]

% Input
\node[layer, fill=white] (input) {\textbf{Input}\\$a(x)$};

% Lifting layer
\node[layer, fill=white, right=0.6cm of input] (lift) {\textbf{P}\\Lift};

% Fourier layers
\node[layer, fill=white, right=0.5cm of lift] (f1) {\textbf{F1}};
\node[layer, fill=white, right=0.4cm of f1] (f2) {\textbf{F2}};
\node[layer, fill=white, right=0.4cm of f2] (f3) {\textbf{F3}};
\node[layer, fill=white, right=0.4cm of f3] (f4) {\textbf{F4}};

% Projection layer
\node[layer, fill=white, right=0.5cm of f4] (proj) {\textbf{Q}\\Proj};

% Output
\node[layer, fill=white, right=0.6cm of proj] (output) {\textbf{Output}\\$u(x)$};

% Main arrows
\draw[arrow] (input) -- (lift);
\draw[arrow] (lift) -- (f1);
\draw[arrow] (f1) -- (f2);
\draw[arrow] (f2) -- (f3);
\draw[arrow] (f3) -- (f4);
\draw[arrow] (f4) -- (proj);
\draw[arrow] (proj) -- (output);

% Skip connections (W) - curved arrows above
\draw[arrow, dashed, red!70] (f1.north) to[out=60, in=120] node[above, font=\tiny] {$W$} (f2.north);
\draw[arrow, dashed, red!70] (f2.north) to[out=60, in=120] (f3.north);
\draw[arrow, dashed, red!70] (f3.north) to[out=60, in=120] (f4.north);

% Equation below
\node[below=0.4cm of f2, font=\scriptsize, text width=4cm, align=center] {$v_{l+1} = \sigma(\mathcal{K}v_l + Wv_l)$};

\end{tikzpicture}%
}
\caption{FNO architecture: lifting (P), four Fourier layers with skip connections (W), and projection (Q).}
\label{fig:fno_architecture}
\end{figure}

\subsection{Monte Carlo Dropout and Deep Ensembles}

MC Dropout \cite{gal2016dropout} estimates uncertainty through $T$ stochastic forward passes with dropout active at test time. The predictive mean and variance are:
\begin{equation}
    \mu = \frac{1}{T}\sum_{t=1}^{T} \hat{y}_t, \quad \sigma^2 = \frac{1}{T}\sum_{t=1}^{T} (\hat{y}_t - \mu)^2
\end{equation}

Deep Ensembles \cite{lakshminarayanan2017simple} train $M$ independent networks and estimate uncertainty from their disagreement:
\begin{equation}
    \mu = \frac{1}{M}\sum_{m=1}^{M} \hat{y}_m, \quad \sigma^2 = \frac{1}{M}\sum_{m=1}^{M} (\hat{y}_m - \mu)^2
\end{equation}

\subsection{Evidential Deep Learning for Regression}

For the Fourier Neural Operator task, we assume the target $y$ is drawn from a Gaussian distribution
$\mathcal{N}(\mu, \sigma^2)$. In the evidential framework, the unknown parameters $\mu$ and $\sigma^2$ are modeled
by a \textit{Normal-Inverse-Gamma} (NIG) distribution:
\begin{multline}
    p(\mu, \sigma^2 | \gamma, v, \alpha, \beta) = \frac{\beta^\alpha \sqrt{v}}{\Gamma(\alpha) \sqrt{2\pi\sigma^2}} \left(\frac{1}{\sigma^2}\right)^{\alpha+1} \\
    \times \exp\left( -\frac{2\beta + v(\gamma - \mu)^2}{2\sigma^2} \right)
\end{multline}
where $\boldsymbol{\theta} = (\gamma, v, \alpha, \beta)$ are the evidential hyperparameters produced by the network. This formulation is mathematically valid because the NIG is the conjugate prior of a Gaussian likelihood with unknown mean and variance. Equivalently, this can be factorized as:
\begin{equation}
    p(\mu, \sigma^2 | \gamma, \nu, \alpha, \beta) = \mathcal{N}\left(\mu; \gamma, \frac{\sigma^2}{\nu}\right) \cdot \text{Inv-Gamma}(\sigma^2; \alpha, \beta)
\end{equation}
where $\gamma \in \mathbb{R}$ is the predicted mean, $\nu > 0$ is the evidence (precision of mean estimate), $\alpha > 1$ is the shape parameter, and $\beta > 0$ is the scale parameter. The predictive distribution marginalizing over the unknown parameters is Student-$t$:
\begin{equation}
    p(y | \gamma, \nu, \alpha, \beta) = \text{Student-}t\left(y; \gamma, \frac{\beta(1+\nu)}{\nu \alpha}, 2\alpha\right)
\end{equation}

The model is trained by minimizing a multi-objective loss function $L = L_{NLL} + \lambda L_{reg}$. The first term,
$L_{NLL}$, is the negative log-likelihood of the model evidence (Type-II Maximum Likelihood), which forces the
distribution to encompass the ground truth $y$. The second term, $L_{reg}$, is an evidence-based penalty that
regularizes the model to report high epistemic uncertainty in regions with low data density.

A key advantage of this approach is the analytical decomposition of total uncertainty into its aleatoric
(data-dependent) and epistemic (model-dependent) components:
\begin{equation}
    \underbrace{\sigma_{ale}^2 = \frac{\beta}{\alpha - 1}}_{\text{Aleatoric}}, \quad \quad \underbrace{\sigma_{epi}^2 = \frac{\beta}{v(\alpha - 1)}}_{\text{Epistemic}}
\end{equation}
Here, $v$ represents the ``virtual observations'' or total evidence supporting the prediction. As $v \to \infty$,
the epistemic uncertainty vanishes, leaving only the inherent noise of the data. While this framework offers a
principled path to real-time uncertainty quantification, our results in Section~\ref{sec:results} suggest a
fundamental trade-off between calibration accuracy and the validity of this decomposition in high-dimensional
spectral mappings.

\begin{figure*}[htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    node distance=0.8cm and 1.2cm,
    box/.style={rectangle, draw, minimum width=2cm, minimum height=1.2cm, align=center, rounded corners=3pt},
    smallbox/.style={rectangle, draw, minimum width=1.5cm, minimum height=0.8cm, align=center, rounded corners=2pt, font=\small},
    arrow/.style={->, >=stealth, thick},
    title/.style={font=\bfseries\large}
]

% ==================== LEFT SIDE: Standard Regression ====================
\node[title] at (2.5, 5) {(a) Standard Regression};

% Input
\node[box, fill=white] (std_input) at (0, 3.5) {
    \textbf{Input}\\
    $x$
};

% Neural Network
\node[box, fill=white, minimum height=2cm, minimum width=2.5cm] (std_net) at (2.5, 3.5) {
    \textbf{Neural}\\
    \textbf{Network}\\
    \footnotesize(FNO $\mathcal{G}_\theta$)
};

% Single Output
\node[box, fill=white] (std_output) at (5, 3.5) {
    \textbf{Output}\\
    $\hat{y}$
};

% Arrows
\draw[arrow, line width=1.5pt] (std_input) -- (std_net);
\draw[arrow, line width=1.5pt] (std_net) -- (std_output);

% Loss function box
\node[draw, fill=white, rounded corners, text width=4cm, align=center] (std_loss) at (2.5, 0.8) {
    \textbf{MSE Loss}\\[3pt]
    $\mathcal{L} = (y - \hat{y})^2$
};

\draw[arrow, dashed, gray] (std_output.south) -- ++(0,-1.0) -| (std_loss.north);

% Output visualization - point estimate
\begin{scope}[shift={(0, -2)}]
    \draw[->] (0,0) -- (5,0) node[right, font=\small] {$y$};
    \draw[->] (0,0) -- (0,1.5) node[above, font=\small] {$p(y)$};

    % Single point (Dirac delta approximation)
    \fill[green!60] (2.5,0) circle (4pt);
    \draw[green!70!black, line width=2pt] (2.5,0) -- (2.5,1.2);
    \node[font=\footnotesize, above] at (2.5,1.2) {$\hat{y}$};

    % Label
    \node[font=\footnotesize, text=gray] at (2.5, -0.5) {Point estimate only};
\end{scope}

% ==================== RIGHT SIDE: Evidential Regression ====================
\node[title] at (10.5, 5) {(b) Evidential Regression};

% Input
\node[box, fill=white] (evd_input) at (8, 3.5) {
    \textbf{Input}\\
    $x$
};

% Neural Network
\node[box, fill=white, minimum height=2cm, minimum width=2.5cm] (evd_net) at (10.5, 3.5) {
    \textbf{Neural}\\
    \textbf{Network}\\
    \footnotesize(FNO $\mathcal{G}_\theta$)
};

% Four output heads
\node[smallbox, fill=white] (gamma) at (13.5, 4.5) {$\gamma$};
\node[smallbox, fill=lime!30] (nu) at (13.5, 3.8) {$\nu$};
\node[smallbox, fill=yellow!40] (alpha) at (13.5, 3.1) {$\alpha$};
\node[smallbox, fill=pink!40] (beta) at (13.5, 2.4) {$\beta$};

% Arrows from network to heads
\draw[arrow, line width=1.5pt] (evd_input) -- (evd_net);
\draw[arrow] (evd_net.east) -- ++(0.3,0) |- (gamma.west);
\draw[arrow] (evd_net.east) -- ++(0.3,0) |- (nu.west);
\draw[arrow] (evd_net.east) -- ++(0.3,0) |- (alpha.west);
\draw[arrow] (evd_net.east) -- ++(0.3,0) |- (beta.west);

% NIG output
\node[box, fill=purple!25, minimum width=2.2cm] (nig_out) at (16, 3.5) {
    \textbf{NIG}\\
    $(\gamma,\nu,\alpha,\beta)$
};

\draw[arrow] (gamma.east) -- ++(0.2,0) |- (nig_out.west);
\draw[arrow] (nu.east) -- ++(0.2,0) |- (nig_out.west);
\draw[arrow] (alpha.east) -- ++(0.2,0) |- (nig_out.west);
\draw[arrow] (beta.east) -- ++(0.2,0) |- (nig_out.west);

% Loss function box
\node[draw, fill=red!15, rounded corners, text width=5cm, align=center] (evd_loss) at (10.5, 0.8) {
    \textbf{Evidential Loss}\\[3pt]
    $\mathcal{L} = \mathcal{L}_{\text{NLL}} + \lambda \cdot \mathcal{L}_{\text{reg}}$
};

\draw[arrow, dashed, gray] (nig_out.south) -- ++(0,-1.0) -| (evd_loss.north);

% Output visualization - distribution
\begin{scope}[shift={(8, -2)}]
    \draw[->] (0,0) -- (5,0) node[right, font=\small] {$y$};
    \draw[->] (0,0) -- (0,1.5) node[above, font=\small] {$p(y)$};

    % Student-t distribution (bell curve)
    \fill[purple!20, smooth] plot coordinates {
        (0.5,0) (0.5,0.05) (1,0.15) (1.5,0.4) (2,0.85) (2.3,1.1) (2.5,1.2)
        (2.7,1.1) (3,0.85) (3.5,0.4) (4,0.15) (4.5,0.05) (4.5,0)
    } -- cycle;
    \draw[purple!70, line width=1.5pt, smooth] plot coordinates {
        (0.5,0.05) (1,0.15) (1.5,0.4) (2,0.85) (2.3,1.1) (2.5,1.2)
        (2.7,1.1) (3,0.85) (3.5,0.4) (4,0.15) (4.5,0.05)
    };

    % Mean line
    \draw[red, dashed, thick] (2.5,0) -- (2.5,1.35);
    \node[font=\footnotesize, red, above] at (2.5,1.35) {$\gamma$};

    % Uncertainty band
    \draw[<->, green!60!black, thick] (1.5,0.5) -- (3.5,0.5);
    \node[font=\tiny, green!60!black, above] at (2.5,0.5) {$\sigma^2_{\text{ale}} + \sigma^2_{\text{epi}}$};

    % Label
    \node[font=\footnotesize, text=purple!70] at (2.5, -0.5) {Full predictive distribution};
\end{scope}

% ==================== Comparison Arrow ====================
\draw[<->, line width=2pt, red!60] (5.8, 3.5) -- (7.2, 3.5);
\node[font=\small, text=red!60, above] at (6.5, 3.7) {vs};

\end{tikzpicture}%
}
\caption{Comparison of standard regression and evidential regression. (a) Standard regression outputs a single point estimate with MSE loss, providing no uncertainty information. (b) Evidential regression outputs four parameters of the Normal-Inverse-Gamma distribution, enabling decomposition of uncertainty into aleatoric (data noise) and epistemic (model uncertainty) components from a single forward pass. The neural network block corresponds to the FNO backbone detailed in Fig.~\ref{fig:evidential_framework}.}
\label{fig:standard_vs_evidential}
\end{figure*}

\subsection{Epistemic and Aleatoric Uncertainty}

Uncertainty decomposes into two types \cite{kendall2017uncertainties,der2009aleatory}:
\begin{itemize}
    \item \textbf{Epistemic uncertainty}: Model uncertainty due to limited data, reducible with more training data
    \item \textbf{Aleatoric uncertainty}: Inherent data noise, irreducible regardless of model or data
\end{itemize}

For the NIG parameterization, these are computed as:
\begin{align}
    \text{Aleatoric:} \quad \sigma^2_{\text{ale}} &= \frac{\beta}{\alpha - 1} \\
    \text{Epistemic:} \quad \sigma^2_{\text{epi}} &= \frac{\beta}{\nu(\alpha - 1)}
\end{align}

\subsection{Evaluation Metrics}

\subsubsection{Predictive Accuracy}

\textbf{Mean Squared Error (MSE)}:
\begin{equation}
    \text{MSE} = \frac{1}{N}\sum_{i=1}^{N} \|y_i - \hat{y}_i\|^2
\end{equation}

\textbf{Relative $L^2$ Error}:
\begin{equation}
    \text{rel\_l2} = \frac{\|y - \hat{y}\|_2}{\|y\|_2}
\end{equation}

\subsubsection{Calibration Quality}

\textbf{Expected Calibration Error (ECE)}: Measures alignment between predicted confidence and actual accuracy by 
binning predictions and computing the weighted average gap. Lower is better (perfect calibration: ECE = 0).

\textbf{Coverage}: Fraction of true values within predicted confidence intervals:
\begin{equation}
    \text{Coverage} = \frac{1}{N}\sum_{i=1}^{N} \mathbb{1}\left[y_i \in [\hat{y}_i - z_\alpha\hat{\sigma}_i, \hat{y}_i + z_\alpha\hat{\sigma}_i]\right]
\end{equation}
Target: 0.95 for 95\% confidence intervals.

\textbf{Interval Width}: Average width of prediction intervals (lower is better, but coverage must be maintained).

\subsubsection{Uncertainty Quality}

\textbf{Uncertainty-Error Correlation}: Pearson correlation between predicted uncertainty and actual error:
\begin{equation}
    \rho = \text{corr}(\hat{\sigma}, |y - \hat{y}|)
\end{equation}
Higher indicates more informative uncertainty.

\textbf{Negative Log-Likelihood (NLL)}:
\begin{equation}
    \text{NLL} = \frac{1}{2N}\sum_{i=1}^{N} \left[\log(2\pi\hat{\sigma}_i^2) + \frac{(y_i - \hat{y}_i)^2}{\hat{\sigma}_i^2}\right]
\end{equation}

\textbf{AUROC for OOD Detection}: Area under ROC curve using uncertainty as decision score. 0.5 = random, 1.0 = 
perfect detection.
